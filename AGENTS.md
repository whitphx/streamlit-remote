## Release Management

For user-visible changes, add a scriv changelog fragment before committing. Do not skip this because no fragment directory is present yet; use the project's scriv configuration to create the fragment.
